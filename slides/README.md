From this directory at the command line, type "make" to generate slides and R scripts.
In addition to raw::InstallSuggestions() you will need to have
  - pandoc installed
  - revealjs installed (from CRAN)
  - rblocks installed ('devtools::install_github("ramnathv/rblocks")')
  
  